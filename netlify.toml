[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback for client-side routing (activation gate lives at /)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
